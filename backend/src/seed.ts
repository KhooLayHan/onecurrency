// import { users, transactions, accounts } from "./schema"

// function seed() {
// console.log("🌱 Starting database seed...");
// TODO: Add your seeding logic here once schema is defined
// Example:
// const userData = Array.from({ length: 10 }, () => ({
//   email: faker.internet.email(),
//   name: faker.person.fullName(),
//   createdAt: faker.date.past(),
// }))
// await db.insert(users).values(userData)
// console.log(`✅ Seeded ${userData.length} users`)
// console.log("⚠️  No seeding logic implemented yet.");
// console.log("   Add your schema imports and seeding logic to this file.");
// console.log("🎉 Database seeding completed!");
// process.exit(0);
// }

// seed().catch((error) => {
//   console.error("❌ Seeding failed:", error);
//   process.exit(1);
// });
