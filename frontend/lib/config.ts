/**
 * config.ts
 *
 * Frontend runtime configuration constants.
 */

/**
 * Active network ID for on-chain operations.
 * 1 = Hardhat (local dev). Override in .env.local for Sepolia (e.g. 11155111).
 */
export const ACTIVE_NETWORK_ID = 1;
